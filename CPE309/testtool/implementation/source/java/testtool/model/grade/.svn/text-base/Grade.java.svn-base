package testtool.model.grade;

import testtool.*;

/****
 *
 * Class Grade is the model class for the test tool grading functionality.  It
 * contains methods for all of the operations dealing with grading a test automatically
 * as well as manually, providing options to leave the student feedback if needed.
 *
 * @author nmock (nmock@calpoly.edu)
 * @version 13jan11
 *
 */
public class Grade {
   public Grade () {
      
   }
   
   /**
    * Allows the instructor to add feedback to a student's particular
    * question.
    */
   public void addFeedback (Question q, Student s) {
      
   }
   
   /**
    * Will automatically grade a particular question from a provided
    * student's test.
    */
   public void autoGradeQuestion (Test t, Student s, Question q) {
      
   }
   
   /**
    * Will automatically grade a student's test based on answers in the
    * database. Will return true if all questions were autograded, false
    * if only a partial set was autograded.
    */
   public boolean autoGradeTest (Test t, Student s) {
      
   }
   
   /**
    * Returns an array of answers containing a test from a particular
    * student.
    */
   public Answers[] getTest (Student s) {
      
   }
   
   /**
    * Returns an array of answers containing a particular question from all
    * students.
    */
   public Answers[] getAnswers (Question q, Test t) {
      
   }
}